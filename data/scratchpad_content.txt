self.timer = QTimer(self)
        self.timer.timeout.connect(self.save_text)  # Connect the timeout signal to the method
        self.timer.start(60000)  # Trigger every 60000 milliseconds (1 minute)self.timer = QTimer(self)
        self.timer.timeout.connect(self.save_text)  # Connect the timeout signal to the method
        self.timer.start(60000)  # Trigger every 60000 milliseconds (1 minute)self.timer = QTimer(self)
        self.timer.timeout.connect(self.save_text)  # Connect the timeout signal to the method
        self.timer.start(60000)  # Trigger every 60000 milliseconds (1 minute)

hjk jk hjk

8968687
sadsad


sadsakjsad

xczxzc

HELLO!!sda


sda


HI



